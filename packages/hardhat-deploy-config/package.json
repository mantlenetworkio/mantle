{
  "name": "@bitdaoio/hardhat-deploy-config",
  "version": "1.0.0",
  "description": "[Bitnetwork] Hardhat deploy configuration plugin",
  "main": "dist/src/index.js",
  "types": "dist/src/index.d.ts",
  "files": [
    "dist/*"
  ],
  "scripts": {
    "test:coverage": "echo 'No tests defined.'",
    "build": "tsc -p tsconfig.json",
    "clean": "rimraf dist/ ./tsconfig.tsbuildinfo",
    "lint": "yarn lint:fix && yarn lint:check",
    "pre-commit": "lint-staged",
    "lint:fix": "yarn lint:check --fix",
    "lint:check": "eslint . --max-warnings=0"
  },
  "keywords": [
    "Bitnetwork",
    "ethereum",
    "hardhat",
    "deploy",
    "config",
    "plugin"
  ],
  "homepage": "https://github.com/bitdaoio/bitnetwork/tree/develop/packages/hardhat-deploy-config#readme",
  "license": "MIT",
  "author": "Bitnetwork PBC",
  "repository": {
    "type": "git",
    "url": "https://github.com/bitdaoio/bitnetwork.git"
  },
  "devDependencies": {
    "ethers": "^5.6.8",
    "ts-node": "^10.8.2",
    "hardhat": "^2.9.6"
  }
}
